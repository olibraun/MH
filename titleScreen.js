class titleScreen{
  constructor(){
  }

  show(){
    fill(0, 255, 0);
    noStroke();
    textAlign(CENTER,CENTER);
    textSize(35);
    textStyle(NORMAL);
    text("MOORHUHN TITLE SCREEN",width/2,height/2);
  }
}
